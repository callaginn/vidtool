# vidtool

A simple video conversion tool. Converts videos to optimized MP4, WebM, and cover images.

## Development server

```bash
composer serve
```

Starts a PHP dev server on the first available port (starting at 8000) with no execution time limit.
