# vidtool

A simple video conversion tool. Converts videos to optimized MP4, WebM, and cover images.

## Development server

```bash
composer serve
```

Starts a PHP dev server at `http://localhost:8000` with no execution time limit.
